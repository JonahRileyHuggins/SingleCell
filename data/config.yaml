# Genome Complete MCF10A Model

# General infos
name: "Human Epithelial Cell Model"
header: "Under Construction"
location: "."
version: "1.0"
description: "Configuration for loading reference and swappable input files"
author: "Jonah R. Huggins"

# Compilation
compilation:
    directory: "."
    files:
      compartments: "SPARCED-Compartments.tsv"
      ratelaws: "GS-SPARCED-Ratelaws.tsv"
      species: "GS-SPARCED-Species.tsv"
      parameters: "GS-SPARCED-Parameters.tsv"
      annotations: "GS-SPARCED-Annotations.tsv"

